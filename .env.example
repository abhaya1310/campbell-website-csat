# Formspree form endpoint id (the part after /f/ in your Formspree form URL).
# Create a free form at https://formspree.io, then set this in Vercel → Settings → Environment Variables.
# Forms (reservation, contact, newsletter) submit to it via AJAX. Empty = forms show a "not configured" notice.
PUBLIC_FORMSPREE_ID=
